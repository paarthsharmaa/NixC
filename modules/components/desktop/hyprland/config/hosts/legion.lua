-- Keep a safe fallback until the exact panel description
-- and supported refresh modes are copied from:
--
-- hyprctl monitors all

hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = 1,
})
